<!doctype html><html lang="fa" dir="rtl"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#10131a"><title>GameForge Studio</title><style>
*{box-sizing:border-box}body{margin:0;font-family:Tahoma,Arial,sans-serif;background:#0b0e14;color:#eef2f7}button,input,textarea,select{font:inherit}button{cursor:pointer;border:0}.app{height:100vh;display:flex;flex-direction:column}.top{height:58px;display:flex;align-items:center;gap:8px;padding:8px 12px;background:#11151e;border-bottom:1px solid #252b38}.brand{font-weight:800;margin-left:auto}.pill{padding:7px 10px;background:#1b2230;border:1px solid #2b3445;border-radius:9px;font-size:12px}.btn{background:#1d2634;color:#fff;border:1px solid #303b4d;border-radius:9px;padding:8px 11px}.btn:hover{background:#253144}.primary{background:#3b82f6;border-color:#3b82f6}.danger{background:#7f2630}.layout{flex:1;min-height:0;display:grid;grid-template-columns:235px minmax(300px,1fr) 285px;direction:ltr}.panel{background:#10141c;border-left:1px solid #252b38;overflow:auto;direction:rtl}.left{border-left:0;border-right:1px solid #252b38}.stage-wrap{display:flex;flex-direction:column;min-width:0;direction:rtl}.toolbar{padding:8px;background:#0f131a;border-bottom:1px solid #252b38;display:flex;gap:7px;align-items:center;flex-wrap:wrap}.stage-area{flex:1;overflow:auto;display:flex;justify-content:center;align-items:center;padding:24px;background:radial-gradient(circle at 50% 20%,#18202d 0,#0b0e14 55%)}.stage{position:relative;background:#fff;color:#111;box-shadow:0 15px 50px #0008;overflow:hidden;touch-action:none}.grid{background-image:linear-gradient(#cbd5e122 1px,transparent 1px),linear-gradient(90deg,#cbd5e122 1px,transparent 1px);background-size:20px 20px}.el{position:absolute;user-select:none;cursor:pointer;overflow:hidden;white-space:pre-wrap;word-break:break-word}.el.selected{outline:2px solid #3b82f6;outline-offset:2px}.g-btn{display:flex;align-items:center;justify-content:center;border-radius:10px;border:2px solid #263247;background:#e8edf5;color:#111}.g-text{display:flex;align-items:center}.g-box{border:2px solid #465164;background:#e8edf5;border-radius:8px}.g-badge{display:flex;align-items:center;justify-content:center;border-radius:999px;background:#3b82f6;color:#fff;font-weight:bold}.section{padding:12px;border-bottom:1px solid #222936}.section h3{margin:0 0 10px;font-size:13px}.toolgrid{display:grid;grid-template-columns:1fr 1fr;gap:7px}.field{margin:8px 0}.field label{display:block;font-size:11px;color:#9da8b8;margin-bottom:5px}.field input,.field textarea,.field select{width:100%;background:#0c1017;color:#fff;border:1px solid #30394a;border-radius:8px;padding:8px;outline:none}.field textarea{min-height:70px;resize:vertical}.row{display:flex;gap:6px}.row>*{flex:1}.muted{font-size:11px;color:#8994a5;line-height:1.6}.list{display:flex;flex-direction:column;gap:6px}.scene-item,.elem-item{display:flex;align-items:center;gap:6px;padding:8px;background:#171d28;border:1px solid #283142;border-radius:8px}.scene-item.active,.elem-item.active{border-color:#3b82f6;background:#17263d}.scene-item span,.elem-item span{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.spacer{flex:1}.modal-back{position:fixed;inset:0;background:#0009;display:none;align-items:center;justify-content:center;padding:18px;z-index:20}.modal-back.show{display:flex}.modal{width:min(560px,100%);max-height:90vh;overflow:auto;background:#111722;border:1px solid #30394a;border-radius:15px;padding:18px;box-shadow:0 30px 80px #000c}.modal h2{margin:0 0 14px}.modal-actions{display:flex;gap:8px;justify-content:flex-start;margin-top:14px}.icon-preview{width:60px;height:60px;border-radius:14px;background:#1d2634;display:flex;align-items:center;justify-content:center;font-size:30px}.toast{position:fixed;bottom:18px;left:50%;transform:translateX(-50%);background:#182130;border:1px solid #334057;padding:9px 14px;border-radius:10px;display:none;z-index:50}.toast.show{display:block}.empty{padding:20px;text-align:center;color:#8290a4;font-size:12px}.small{font-size:10px}.action-extra{padding:8px;background:#0c1119;border:1px solid #273143;border-radius:8px;margin-top:7px}.preview-stage{width:min(90vw,420px);height:min(82vh,760px);position:relative;background:#fff;overflow:hidden;color:#111;border-radius:10px}.preview-modal .modal{width:min(620px,100%)}.hidden{display:none!important}@media(max-width:900px){.layout{grid-template-columns:190px minmax(260px,1fr) 235px}.top .pill{display:none}}@media(max-width:700px){.app{height:auto;min-height:100vh}.top{height:auto;min-height:58px;flex-wrap:wrap}.layout{display:flex;flex-direction:column}.left,.panel{max-height:none}.left{order:2}.stage-wrap{order:1;height:60vh;min-height:420px}.panel.right{order:3}.toolgrid{grid-template-columns:repeat(4,1fr)}.section{padding:10px}.left .section:nth-child(2) .toolgrid{grid-template-columns:1fr 1fr}.stage-area{padding:10px}.stage{transform-origin:center center}}

/* Targeted editor fix: elements can be moved in either portrait or landscape. */
.stage-canvas { touch-action: none; user-select: none; }
.stage-element { touch-action: none; cursor: grab; }
.stage-element.dragging { cursor: grabbing; }

.custom-size-controls{display:flex;align-items:center;gap:8px;flex-wrap:wrap;margin-top:10px}
.custom-size-controls input{width:90px}
.custom-size-controls button{padding:7px 10px} 

 
.score-hud{
 position:absolute;z-index:9999;top:12px;right:12px;
 padding:9px 15px;border-radius:12px;background:rgba(0,0,0,.78);
 color:#fff;font-weight:800;font-size:18px;direction:rtl;
 pointer-events:none;display:none
}
.score-action-box{margin-top:10px;padding:10px;border:1px solid rgba(255,255,255,.12);border-radius:10px}
</style></head><body><div class="app">
<header class="top"><div class="brand">🎮 GameForge Studio</div><span class="pill" id="projectLabel">پروژه جدید</span><button class="btn" data-action="new">پروژه جدید</button><button class="btn" data-action="save">ذخیره</button><button class="btn primary" data-action="preview">▶ اجرا</button></header>
<div class="layout">
<aside class="panel left"><div class="section"><h3>ابزارها</h3><div class="toolgrid"><button class="btn" data-add="button">🔘 دکمه</button><button class="btn" data-add="text">🔤 متن</button><button class="btn" data-add="box">⬛ کادر</button><button class="btn" data-add="badge">🏷️ برچسب</button></div></div><div class="section"><h3>صحنه‌ها</h3><div id="sceneList" class="list"></div><button class="btn primary" style="width:100%;margin-top:8px" data-action="addScene">＋ صحنه جدید</button></div><div class="section"><h3>مدیریت</h3><div class="toolgrid"><button class="btn" data-action="duplicate">کپی</button><button class="btn danger" data-action="delete">حذف</button><button class="btn" data-action="clear">پاک‌سازی</button><button class="btn" data-action="grid">شبکه</button></div></div><div class="section"><h3>خروجی</h3><button class="btn" style="width:100%;margin-bottom:7px" data-action="exportHtml">⬇️ خروجی HTML</button><button class="btn" style="width:100%;margin-bottom:7px" data-action="exportZip">📦 خروجی ZIP</button><button class="btn primary" style="width:100%" data-action="install">📱 انتشار / نصب</button></div></aside>
<main class="stage-wrap"><div class="toolbar"><button class="btn" data-orient="portrait">📱 عمودی</button><button class="btn" data-orient="landscape">🖥️ افقی</button><button class="btn" data-action="projectSettings">⚙️ تنظیمات پروژه</button><span class="spacer"></span><span class="muted" id="stageInfo"></span></div><div class="stage-area"><div id="stage" class="stage"></div></div></main>
<aside class="panel right"><div class="section"><h3>بازرس عنصر</h3><div id="inspector"></div></div><div class="section"><h3>لیست عناصر</h3><div id="elementList" class="list"></div></div></aside>
</div></div>
<div id="modalBack" class="modal-back"><div id="modal" class="modal"></div></div><div id="toast" class="toast"></div>
<script>
'use strict';
const KEY='gameforge_studio_v5';
let state={project:null,sceneId:null,selectedId:null,grid:true};
const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
function uid(p='id'){return p+Math.random().toString(36).slice(2,8)+Date.now().toString(36).slice(-4)}
function esc(s){return String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
function defProject(){let sid=uid('scene');return {name:'بازی من',author:'',description:'',icon:'🎮',orientation:'portrait',width:360,height:640,startScene:sid,showScore:true,initialScore:0,scenes:[{id:sid,name:'مرحله ۱',bg:'#ffffff',elements:[]} ]}}
function currentScene(){return state.project?.scenes.find(s=>s.id===state.sceneId)||null}
function selected(){let s=currentScene();return s?.elements.find(e=>e.id===state.selectedId)||null}
function ensure(){if(!state.project)state.project=defProject();if(!state.sceneId)state.sceneId=state.project.startScene;if(!currentScene())state.sceneId=state.project.scenes[0].id}
function toast(t){let x=$('#toast');x.textContent=t;x.classList.add('show');clearTimeout(toast.t);toast.t=setTimeout(()=>x.classList.remove('show'),1800)}
function save(silent=false){localStorage.setItem(KEY,JSON.stringify(state.project));if(!silent)toast('پروژه ذخیره شد ✓')}
function load(){try{let p=JSON.parse(localStorage.getItem(KEY));if(p){state.project=p;state.sceneId=p.startScene;return true}}catch{}return false}
function openModal(html){$('#modal').innerHTML=html;$('#modalBack').classList.add('show')}
function closeModal(){ $('#modalBack').classList.remove('show'); $('#modal').innerHTML=''}
function projectWizard(existing=false){let p=state.project||defProject();openModal(`<h2>${existing?'تنظیمات پروژه':'ساخت پروژه جدید'}</h2><div class="field"><label>نام بازی *</label><input id="wName" value="${esc(p.name)}"></div><div class="field"><label>سازنده</label><input id="wAuthor" value="${esc(p.author)}"></div><div class="field"><label>توضیح</label><textarea id="wDesc">${esc(p.description)}</textarea></div><div class="field"><label>آیکون بازی (ایموجی)</label><div class="row"><input id="wIcon" maxlength="4" value="${esc(p.icon)}"><div class="icon-preview" id="iconPrev">${esc(p.icon)}</div></div></div><div class="field"><label>حالت نمایش</label><select id="wOri"><option value="portrait" ${p.orientation==='portrait'?'selected':''}>عمودی (360×640)</option><option value="landscape" ${p.orientation==='landscape'?'selected':''}>افقی (640×360)</option></select></div><div class="field"><label>امتیاز شروع</label><input id="wScore" type="number" value="${p.initialScore||0}"></div><div class="modal-actions"><button class="btn primary" data-action="wizardSave">ساخت / ذخیره</button><button class="btn" data-action="closeModal">لغو</button></div>`);$('#wIcon').addEventListener('input',()=>$('#iconPrev').textContent=$('#wIcon').value||'🎮')}
function newProject(){state.project=defProject();state.sceneId=state.project.startScene;state.selectedId=null;projectWizard(false)}
function addScene(){let n=state.project.scenes.length+1,s={id:uid('scene'),name:'مرحله '+n,bg:'#ffffff',elements:[]};state.project.scenes.push(s);state.sceneId=s.id;state.selectedId=null;render();save(true);toast('صحنه ساخته شد')}
function addElement(type){let s=currentScene();if(!s)return;let i=s.elements.length,e={id:uid('el'),type,text:type==='button'?'دکمه '+(i+1):type==='text'?'متن جدید':type==='badge'?'برچسب':'',x:30+(i%4)*55,y:40+Math.floor(i/4)*70,w:type==='text'?180:type==='box'?220:type==='badge'?90:150,h:type==='text'?45:type==='box'?110:type==='badge'?42:55,bg:type==='button'?'#e8edf5':type==='box'?'#e8edf5':type==='badge'?'#3b82f6':'transparent',color:type==='badge'?'#fff':'#111',fontSize:type==='text'?20:15,borderRadius:type==='badge'?999:10,action:{type:'none'}};s.elements.push(e);state.selectedId=e.id;render();toast('عنصر اضافه شد')}
function updateSelected(k,v){let e=selected();if(!e)return;e[k]=v;renderStage();renderInspector();renderElementList()}
function render(){ensure();let p=state.project,s=currentScene();$('#projectLabel').textContent=p.name;$('#stageInfo').textContent=`${p.width}×${p.height} • ${s.name}`;$('#stage').style.width=p.width+'px';$('#stage').style.height=p.height+'px';$('#stage').style.background=s.bg;$('#stage').classList.toggle('grid',state.grid);renderStage();renderScenes();renderInspector();renderElementList()}
function renderStage(){let stage=$('#stage');stage.innerHTML='';let s=currentScene();if(!s)return;for(let e of s.elements){let d=document.createElement('div');d.className='el '+(e.type==='button'?'g-btn':e.type==='text'?'g-text':e.type==='box'?'g-box':'g-badge')+(e.id===state.selectedId?' selected':'');d.dataset.id=e.id;d.textContent=e.text||'';Object.assign(d.style,{left:e.x+'px',top:e.y+'px',width:e.w+'px',height:e.h+'px',background:e.bg,color:e.color,fontSize:e.fontSize+'px',borderRadius:e.borderRadius+'px',border:e.type==='text'?'none':undefined});d.addEventListener('pointerdown',ev=>{if(ev.button!==undefined&&ev.button!==0)return;state.selectedId=e.id;render();});stage.appendChild(d)}}
function renderScenes(){let box=$('#sceneList');box.innerHTML='';for(let s of state.project.scenes){let d=document.createElement('div');d.className='scene-item '+(s.id===state.sceneId?'active':'');d.innerHTML=`<span style="flex:1">${esc(s.name)}</span><button class="btn small" data-scene-settings="${s.id}">⚙</button>`;d.addEventListener('click',ev=>{if(ev.target.closest('button'))return;state.sceneId=s.id;state.selectedId=null;render()});box.appendChild(d)}}
function actionOptions(a){return `<option value="none" ${a==='none'?'selected':''}>هیچ اتفاقی</option><option value="scene" ${a==='scene'?'selected':''}>رفتن به صحنه</option><option value="scorePlus" ${a==='scorePlus'?'selected':''}>افزایش امتیاز</option><option value="scoreMinus" ${a==='scoreMinus'?'selected':''}>کاهش امتیاز</option><option value="setScore" ${a==='setScore'?'selected':''}>تنظیم امتیاز</option><option value="message" ${a==='message'?'selected':''}>نمایش پیام</option><option value="restart" ${a==='restart'?'selected':''}>شروع دوباره بازی</option><option value="changeText" ${a==='changeText'?'selected':''}>تغییر متن یک عنصر</option><option value="show" ${a==='show'?'selected':''}>نمایش عنصر</option><option value="hide" ${a==='hide'?'selected':''}>مخفی کردن عنصر</option>`}
function extraHtml(e){let a=e.action||{type:'none'},t=a.type||'none';if(t==='scene')return `<div class="field"><label>صحنه مقصد</label><select id="aScene">${state.project.scenes.map(s=>`<option value="${s.id}" ${a.sceneId===s.id?'selected':''}>${esc(s.name)}</option>`).join('')}</select></div>`;if(['scorePlus','scoreMinus','setScore'].includes(t))return `<div class="field"><label>مقدار امتیاز</label><input id="aAmount" type="number" value="${a.amount??1}"></div>`;if(t==='message')return `<div class="field"><label>متن پیام</label><textarea id="aMessage">${esc(a.message||'آفرین!')}</textarea></div>`;if(['changeText','show','hide'].includes(t))return `<div class="field"><label>عنصر مقصد</label><select id="aTarget">${currentScene().elements.filter(x=>x.id!==e.id).map(x=>`<option value="${x.id}" ${a.targetId===x.id?'selected':''}>${esc(x.text||x.type)}</option>`).join('')}</select></div>${t==='changeText'?`<div class="field"><label>متن جدید</label><textarea id="aNewText">${esc(a.newText||'متن تغییر کرد')}</textarea></div>`:''}`;return ''}
function renderInspector(){let box=$('#inspector'),e=selected();if(!e){box.innerHTML='<div class="empty">یک عنصر از صحنه انتخاب کن.</div>';return}let a=e.action||{type:'none'};box.innerHTML=`<div class="field"><label>متن</label><textarea id="iText">${esc(e.text)}</textarea></div><div class="row"><div class="field"><label>عرض</label><input id="iW" type="number" value="${e.w}"></div><div class="field"><label>ارتفاع</label><input id="iH" type="number" value="${e.h}"></div></div><div class="row"><div class="field"><label>X</label><input id="iX" type="number" value="${e.x}"></div><div class="field"><label>Y</label><input id="iY" type="number" value="${e.y}"></div></div><div class="row"><div class="field"><label>پس‌زمینه</label><input id="iBg" type="color" value="${/^#[0-9a-f]{6}$/i.test(e.bg)?e.bg:'#ffffff'}"></div><div class="field"><label>رنگ متن</label><input id="iColor" type="color" value="${/^#[0-9a-f]{6}$/i.test(e.color)?e.color:'#111111'}"></div></div><div class="row"><div class="field"><label>اندازه متن</label><input id="iFont" type="number" value="${e.fontSize}"></div><div class="field"><label>گردی گوشه</label><input id="iRadius" type="number" value="${e.borderRadius}"></div></div><div class="field"><label>وقتی این دکمه زده شد چه اتفاقی بیفتد؟</label><select id="iAction">${actionOptions(a.type)}
<option value="score+">➕ یک امتیاز اضافه شود</option>
<option value="score-">➖ یک امتیاز کم شود</option>
<option value="setScore">🎯 امتیاز روی عدد مشخص قرار بگیرد</option>
</select>
<div class="score-action-box" id="scoreActionBox">
  <label>وقتی این دکمه زده شد:</label>
  <input id="scoreAmount" type="number" value="1" step="1" placeholder="مقدار امتیاز">
  <label><input id="scoreMessageEnabled" type="checkbox"> پیام هم نشان بده</label>
  <input id="scoreMessage" type="text" placeholder="مثلاً: امتیازت یکی اضافه شد 🎉">
</div>
</div><div id="actionExtra" class="action-extra">${extraHtml(e)}</div>`;bindInspector()}
function bindInspector(){let map={iText:['text',v=>v],iW:['w',Number],iH:['h',Number],iX:['x',Number],iY:['y',Number],iBg:['bg',v=>v],iColor:['color',v=>v],iFont:['fontSize',Number],iRadius:['borderRadius',Number]};for(let id in map){let el=$('#'+id);el.addEventListener('input',()=>{let e=selected();if(!e)return;e[map[id][0]]=map[id][1](el.value);renderStage();renderElementList()})}$('#iAction').addEventListener('change',()=>{let e=selected();e.action={type:$('#iAction').value};renderInspector();toast('رویداد ذخیره شد')});for(let id of ['aScene','aAmount','aMessage','aTarget','aNewText']){let x=$('#'+id);if(x)x.addEventListener('input',()=>syncAction())}}
function syncAction(){let e=selected();if(!e)return;let t=$('#iAction')?.value||'none',a=e.action||{};a.type=t;if($('#aScene'))a.sceneId=$('#aScene').value;if($('#aAmount'))a.amount=Number($('#aAmount').value||0);if($('#aMessage'))a.message=$('#aMessage').value;if($('#aTarget'))a.targetId=$('#aTarget').value;if($('#aNewText'))a.newText=$('#aNewText').value;e.action=a;renderStage()}
function renderElementList(){let box=$('#elementList'),s=currentScene();box.innerHTML='';if(!s.elements.length){box.innerHTML='<div class="empty">هنوز عنصری اضافه نشده.</div>';return}for(let e of s.elements){let d=document.createElement('div');d.className='elem-item '+(e.id===state.selectedId?'active':'');d.innerHTML=`<span style="flex:1">${esc(e.text||e.type)}</span><button class="btn small" data-pick="${e.id}">انتخاب</button>`;box.appendChild(d)}}
function sceneSettings(id){let s=state.project.scenes.find(x=>x.id===id);if(!s)return;openModal(`<h2>تنظیمات ${esc(s.name)}</h2><div class="field"><label>نام صحنه</label><input id="sName" value="${esc(s.name)}"></div><div class="field"><label>پس‌زمینه</label><input id="sBg" type="color" value="${/^#[0-9a-f]{6}$/i.test(s.bg)?s.bg:'#ffffff'}"></div><label class="muted"><input id="sStart" type="checkbox" ${state.project.startScene===s.id?'checked':''}> صحنه شروع بازی باشد</label><div class="modal-actions"><button class="btn primary" data-action="saveSceneSettings" data-id="${id}">ذخیره</button><button class="btn" data-action="closeModal">لغو</button></div>`)}
function applyWizard(){let name=$('#wName').value.trim();if(!name){toast('نام بازی را وارد کن');return}let p=state.project||defProject();p.name=name;p.author=$('#wAuthor').value.trim();p.description=$('#wDesc').value.trim();p.icon=$('#wIcon').value.trim()||'🎮';p.orientation=$('#wOri').value;p.width=p.orientation==='portrait'?360:640;p.height=p.orientation==='portrait'?640:360;p.initialScore=Number($('#wScore').value||0);closeModal();render();save(true);to
